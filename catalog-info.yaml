apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: backstage
  description: Internal developer portal for polarpoint.io — GitHub App auth, GitOps-org catalog discovery, Kubernetes plugin against the tooling cluster.
  annotations:
    github.com/project-slug: polarpoint-io/backstage
    backstage.io/techdocs-ref: dir:.
spec:
  type: website
  owner: group:sre-team
  system: backstage
  lifecycle: production
